# before write_json in export_scene_manifest, check usage context:
# usage context example:
# usage = {"earth_only": True, "attribution": bundle.get("license", {}).get("attribution") is not None}
# (Stubbed; expand later with real pack references)
